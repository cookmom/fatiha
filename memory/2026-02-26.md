# 2026-02-26 Daily Notes

## Morning Session — OpenClaw Update + Context Engineering + Major Site Refactor

### OpenClaw Updated to 2026.2.25
- WSL2 SSL cipher issue persists — `NODE_OPTIONS=--openssl-config=/dev/null` didn't work this time
- Tawfeeq found the fix: `sudo NODE_OPTIONS="" npm install -g openclaw@latest`
- Documented: the empty string NODE_OPTIONS works better than the /dev/null trick

### Context Engineering Skills Added
- Fetched from `muratcankoylan/Agent-Skills-for-Context-Engineering` (GitHub)
- Created `skills/context-compression/SKILL.md` and `skills/context-optimization/SKILL.md`
- Adapted for our workflow, audited with security lens:
  - Never leak MEMORY.md/USER.md/credentials to sub-agents
  - KV-cache section warns about provider persistence
  - Observation masking ≠ security (data already sent)
- Key insight: sub-agent partitioning is #1 lever for credit burn

### Model Config Changed
- Main session → `anthropic/claude-opus-4-6` (was sonnet)
- Sub-agents stay on `claude-max/claude-sonnet-4` (via Max proxy port 3456)
- Cron stays on `anthropic/claude-sonnet-4-6`

### agiftoftime.app — Major Architecture Change (v5 → v6 WIP)

#### Full-Page Background Clock (#8+#9)
- Canvas now `position:fixed; inset:0; z-index:0` — always full viewport
- Removed `window._clockContainer` pattern — no more container sizing
- Removed canvas-moving logic in fullscreen open/close
- Fullscreen: hides page content (`.clock-sticky`, `.scroll-section`), canvas stays at z-index 0, overlay at z-index 200 transparent
- Landing: content sections transparent, clock visible behind all text
- Unified camera: both landing and fullscreen use same camera `pos:[0,10.5,16] fov:35`

#### Full Dark Theme
- `:root` vars switched to dark: `--bg:#0d0d12`, `--text:rgba(232,228,220,.9)`, `--text-muted:rgba(232,228,220,.5)`
- All sections transparent background — clock shows through everywhere
- Nav, footer, sticky CTA, buttons all updated for dark-on-dark
- Night mode section removed entirely, `setNightMode()` is now no-op
- `#sec-hook` gradient removed — fully transparent

#### Tawaf Rotation (#10)
- Cube rotates CCW in its own `cubeGroup` inside `prismGroup`
- Hands stay fixed in `prismGroup` — not affected by cube rotation
- Speed: `TAU / 420` (1 revolution per 7 minutes — 7 heavens)
- 12 o'clock SpotLight at (0, 3.5, -3.0) intensity 18, catches top edge as cube turns

#### Dev Tools Panel (built by sub-agent, enhanced in main)
- Toggle: `D` key or `?dev` URL param
- **Draggable** panel (pointer events on title bar)
- Time: 24h slider, speed buttons (pause/1×/10×/60×/360×/live) with active highlighting
- Camera: OrbitControls (static import from import map), pos/fov/target readouts, FOV slider
- DOF: BokehPass via EffectComposer, focus/focal-length/aperture sliders, toggle button
- Focal length ↔ FOV conversion (36mm full-frame sensor)
- Day/Night: slider with 🌙/☀️ labels, lerps all scene properties
- Tawaf: speed slider + spotlight intensity
- Grain: paper noise overlay (pre-rendered 256×256 tile, CSS `mix-blend-mode:soft-light`), opacity slider + blend mode cycling
- Composition guides: center crosshair, rule of thirds, golden ratio (φ), baroque diagonals — all toggleable independently
- Snapshot: copies all values as JSON to clipboard
- Reset All: returns everything to defaults

#### Key Bug Fixes
- Nested template literal `${TAWAF_SPEED.toFixed(5)}` inside `p.innerHTML` caused "missing ) after argument list" — replaced with hardcoded value
- OrbitControls: dynamic `import()` from CDN failed (import maps don't resolve for dynamic imports in all browsers) — switched to static import
- Fullscreen black: canvas z-index 201 covered overlay z-index 200 chrome — fixed by keeping canvas at z-index 0 and hiding page content instead
- Dangling `else setNightMode(false)` after removing night mode if-chain

#### Local Dev Setup
- Server: `python3 -m http.server 7747 --bind 0.0.0.0` in ramadan-clock-site dir
- WSL2 mirrored networking: Windows accesses via `localhost:7747` after `netsh portproxy` (admin PowerShell required)
- Cloudflare tunnel also works: `/tmp/cloudflared tunnel --url http://localhost:7747`
- `setsid` + `disown` needed to keep python server alive across exec sessions

### Still TODO
- [ ] Prayer-driven lighting (#4) — colored spotlights per prayer window
- [ ] Hour hand takes prayer window color + 3x width (#3)
- [ ] Easter egg interactions when hand hits prayer time (#2)
- [ ] Content text integration — Tawfeeq reviewing contrast/readability
- [ ] PWA manifest for iOS
- [ ] Push to GitHub once local iteration complete

## Session 2 — Feb 26 afternoon (1:26 PM)

### agiftoftime.app — Major Updates

**Full-page background clock (#8+#9) refinements:**
- Fixed `#dialHero` ghost box (set display:none)
- Added landing prayer bar (hijri date + prayer times) fixed below nav
- Scroll sections fade out before reaching cube (scroll-based opacity, deadzone at top 55%)
- Nav bar set to always transparent (no more dark bg on scroll)
- Theme-color meta tag now dynamically sampled from top-left canvas pixel every ~60 frames — status bar blends with 3D scene

**adhan-js integration (prayer times):**
- Replaced AlAdhan API with local adhan-js (16KB UMD from jsdelivr CDN) for instant prayer time calculation
- Zero network latency — computes from lat/lon + sun position math
- Hijri date still fetched from AlAdhan API (background, non-blocking)
- 3 IP geolocation services race in parallel (ipapi.co, ipbase.com, geojs.io) + GPS
- NO hardcoded LA fallback — only real location data
- `SunnahTimes` provides `Midnight` key needed by glass-cube-clock beams
- `window._prayerTimings` / `window._prayerTimingsReady` interface unchanged

**Prayer-driven lighting (#4):**
- 7 prayer presets in `PRAYER_LIGHT` object (Tahajjud/Fajr/Dhuha/Dhuhr/Asr/Maghrib/Isha)
- Each has: bg, fog, fogDensity, ambient, gobo, back, rim, cubeSun + light colors
- Smooth transitions: 80% of window stays in current mood, last 20% smoothstep blends to next
- Currently DISABLED for lookdev — `_applyPrayerLighting` commented out in animation loop
- Prayer beams also DISABLED for lookdev (fills + boundary beams commented out)

**Tawaf rotation changed:**
- Cube no longer rotates — stays static
- Rim light orbits CCW around cube at tawaf speed (radius 4.0, y=5.5)
- Creates rotating specular highlight on cube edges

**Lighting base values (Tawfeeq's lookdev):**
- Back: intensity 10, color #4040a0
- Gobo: intensity 72, color #ff00ff (magenta)
- Rim: intensity 11, color #8060c0
- CubeSun: intensity 105
- Ambient: 0.58
- Tawaf Spot: intensity 12
- BG/Fog: #303030

**Camera:**
- Landing+Fullscreen: pos(0.2, 9.7, 15.0) fov:35 lookAt(0, -0.8, 1.0)
- OrbitControls target synced to camera preset lookAt (no more jump on dev open)
- `_devRef` pattern to avoid temporal dead zone with `_dev` const

**Dev panel additions:**
- Prayer time quick-jump buttons (Tahajjud/Fajr/Dhuha/Dhuhr/Asr/Maghrib/Isha)
- 💡 Lights section: intensity sliders for all lights + color pickers (Back/Gobo/Rim/BG)
- Prayer lighting skipped in dev mode so manual tweaks stick
- Cube world position readout added

**Bug fixes:**
- `ptParseMin` crash on undefined (missing `Midnight` key) — added null guard
- Service worker caching chrome-extension URLs — skip check added
- bigdatacloud 403 / CORS failures — replaced with working IP services
- requestFullscreen without user gesture — guard with `navigator.userActivation`
- favicon 404 — added `<link rel="icon" href="data:,">`

### Rules Updated
- **Rule 0c expanded**: After EVERY code change, use GPU Chrome (puppeteer-core + RTX A6000) to test AND check console. Never use headless OpenClaw browser. Test multiple states for scroll/interaction features.
- **Lesson #6 added** to lessons.md

### Key Decisions
- adhan-js for local prayer time computation (no API dependency for times)
- Prayer lighting as tint/shift relative to base (not absolute override) — approach agreed, not yet implemented
- Rim light orbit instead of cube rotation for tawaf effect
- Full viewport screenshots (no cropping) — Tawfeeq can't see cropped compositions
- RoundedBoxGeometry tried and reverted — Tawfeeq prefers sharp cube edges

### Floor Ray Gap Fix (1:30 PM)
- Sharp visible lines at cube base identified as `floorRay` clock hand beams starting at origin (Y=0)
- Fix: `GAP = 0.8` in `floorRay()` — geometry starts outside cube edge instead of at origin
- Changed `g.translate(0, len/2, 0)` → `g.translate(0, GAP + (len-GAP)/2, 0)` with shortened `PlaneGeometry`
- Cube ~1 unit, rotated 45° so corner extends ~0.7 units; 0.8 provides clearance
- Screenshot taken but visual verification inconclusive

### Next Steps
1. Verify floor ray gap fix visually
2. Re-enable prayer lighting as relative tints on Tawfeeq's base values
3. Re-introduce prayer beams one by one for lookdev
4. Hour hand takes prayer window color (#3)
5. Easter egg interactions (#2)
6. PWA manifest

## Session 3 — Feb 26 evening (6:42 PM → 8:25 PM)

### Transcript Audit
- Spawned sub-agent to parse full 12MB JSONL transcript line by line
- First audit was too shallow — missed details like `_themeFrameCount = 3599` vs `0`, `>= 3600` vs `>= 60`
- Tawfeeq furious about relying on summaries — MUST check actual transcripts
- Second deep audit (531 edits parsed) found: 1 bug (ptParseMin null guard), 2 visual issues (floor geo + polygonOffset)
- Deep audit report: `/home/openclaw-agent/.openclaw/workspace/transcript-audit-deep.md`

### Fixes Applied This Session
All committed locally (not pushed):

1. **FBO render target 4x MSAA** (`e4bdf90`) — `samples: 4` on fboRT for cube edge antialiasing
2. **Theme-color pixel sampling** (`798fba5`, `0dc12ea`):
   - Samples top-left pixel via `gl.readPixels` every ~minute (3600 frames)
   - `_themeFrameCount = 3599` — fires immediately on first frame, then every minute
   - Initial meta tag `#0d0d12` matches scene BG — no color flash on refresh
3. **Floor geometry** (`10bce8d`) — `CircleGeometry(80,128)` → `CircleGeometry(40,64)` + `polygonOffset: true, factor:1, units:1`
4. **Rolling prayer windows** (`fa99781`, `506676d`):
   - 3 discs: active + next upcoming + one after next
   - NEVER 7 discs. NEVER overlap. Only show what fits on 12hr dial.
   - Rolling forward from current prayer — earlier windows hidden
5. **Stepped intensity** (`ad33cef`, `1c3fe70`):
   - Production: `OP_ACTIVE=1.3`, `OP_STEP=0.4` → cascade 1.3 / 0.9 / 0.5
   - Dev mode: all 3 at same intensity (1.3) for testing
6. **Prayer beam flat-top** — `normDist - 0.97` (was 0.90 → 0.95 → 0.97) with `* 12.0` rolloff
7. **Dev panel rebuilt** (`6f059ef`, `86aab5e`, `ef27861`, `3e04c9a`):
   - Toggle: `D` key or `?dev` URL param
   - Time slider (0-1439 min) with Live checkbox
   - Prayer window list with **Start/Mid/End** buttons per prayer — click to jump time
   - Jumps snap instantly (no slow lerp fade)
   - Custom debug windows: add/remove, color picker, start/end time inputs
   - Boundary lines toggle (thin white beams at each window edge)
   - Intensity sliders: OP_ACTIVE and OP_STEP adjustable live
   - `var` not `let` for dev variables — avoids TDZ crash (render loop runs before dev panel code)
8. **Hands from cube center** — `GAP = 0.8` removed, beams originate from center

### Current Code Values (glass-cube-clock.js, commit `1c3fe70`)
- `OP_ACTIVE = 1.3`
- `OP_STEP = 0.4`
- `SECTOR_RADIUS = 9.12`
- `CUBE_Y = 0.60`
- Camera: `pos:[0.2, 9.7, 15.0] fov:35 lookAt:[0, -0.8, 1.0]` (unified landing+fullscreen)
- Floor: `CircleGeometry(40, 64)` with polygonOffset
- FBO: `samples: 4`
- Flat-top: `normDist - 0.97`
- Theme sampling: every 3600 frames, init at 3599
- Second hand: white (`0xffffff` / `0xcccccc`)
- Scene BG/Fog: `0x0d0d12`
- Fog density: `0.048`
- Meta theme-color initial: `#0d0d12`

### Git State
- Last pushed: `bca2e8c` (v5)
- Latest local: `1c3fe70` (25+ local commits ahead)
- NOT pushed to remote yet

### Key Design Decisions Made Tonight
1. **Rolling prayer windows = Islamic mental model**: current + next + after next. "What prayer am I in/approaching?" Urgency on current window.
2. **NEVER 7 discs, NEVER overlap** — only show what fits on 12hr dial, rolling forward
3. **Stepped intensity creates visual hierarchy** — active brightest, fading forward
4. **Dev panel for prayer time testing** — click any prayer to see rolling windows at that time
5. **Theme-color: immediate first sample, then every minute** — not every second (expensive), not every frame
6. **12-hour dial is fine for now** — Tahajjud wrapping acknowledged but acceptable
7. **Adjacent window gap fix via flat-top 0.97** — NOT via uniform overscan, NOT via depthTest:false

### Still TODO
- [ ] Push to GitHub (25+ local commits)
- [ ] Re-enable prayer-driven lighting (7 presets, 80/20 blend) — lost in checkout
- [ ] 24-hour prayer dial — deferred, 12hr is acceptable for now
- [ ] Hour hand takes prayer window color + 3x width (#3)
- [ ] Easter egg interactions (#2)
- [ ] ptParseMin null guard (audit found missing)
- [ ] Full dev panel rebuild (old one had OrbitControls, DOF, grain, composition guides — current one is prayer-focused only)
