# 2026-02-17 — Ramadan Eve Session

## A Gift of Time — Major Changes Today

### Production Workflow Fix (CRITICAL)
- **agiftoftime.app serves `clock.js` (loaded by `index.html`), NOT `index-threejs.html`**
- `clock.js` has CONTAINER MODE code (`window._clockContainer`) that the landing page needs — never replace it wholesale from index-threejs.html
- Always edit `clock.js` surgically, never copy from index-threejs.html
- Cleaned up production repo: removed index-threejs.html, index-threejs-backup.html, index-backup.html, clock-backup.js, font-compare.html
- **Single source of truth: `clock.js`** — bump `?v=XX` in index.html on every push
- Current version: v42

### Visual Changes
- Removed subdial colored rings (Ressence-style) — looked out of place
- NOMOS texture: changed from radial sunray → proper sandblasted/galvanized (uniform fine grain, isotropic, 1024px tiled 4x)
- Dial material: roughness 0.92, metalness 0, normalScale 0.35, envMapIntensity 0.5
- Exposure bumped 0.75 → 0.825
- Main sapphire crystal dome: ADDED then REMOVED — caused visible grey edge ring on mobile. Needs different approach (flat disc?) if revisited
- bgPlane changed to MeshBasicMaterial (unlit) to match scene.background
- bgPlane hidden in fullscreen mode (scene.background is enough)
- bgPlane expanded to 5000 units for screen coverage

### Background Color Matching
- readPixels approach: must happen AFTER render, sample from top-left corner (0, height-1)
- ACES tonemapping math approximation didn't match Three.js exactly
- Final solution: readPixels post-render from background-only pixel

### Fullscreen Mode
- Tour animations now pause when entering fullscreen (exposed `window._stopTour`)
- Idle timer doesn't restart while in fullscreen
- Scale race condition: added `requestAnimationFrame` resize after canvas move + scale enforcement in `onResize`
- Exposed `window._clockOnResize` for external triggering

### Fullscreen Header (NEW)
- Top: Hijri date (from Aladhan API) + location + prayer times, centered
- Positioned at `safe-area + 48px` to clear × and 🌙 buttons
- Gregorian date removed from fullscreen
- Location: GPS + IP race (parallel), reverse geocode upgrade, 6s LA fallback
- IP geolocation via ipapi.co (Aladhan `city=auto` is INVALID)
- Aladhan API `data.date.hijri` has day, month.en, month.ar, year

### Fullscreen Dial Picker (NEW)
- Replaced dot-only bar with arrow picker matching landing page (‹ Name ›)
- Color names (SLATE, TENNIS etc) removed — Surah names are the dial names
- "Listen to this Surah" button below picker
- `fsPrevDial()` / `fsNextDial()` functions

### Tawfeeq Notes
- Ramadan starts tomorrow (Feb 18) — 29 Sha'ban today
- Currently in LA area (shows on 5G cellular, WiFi routes through Vegas)
- Prefers Hijri date prominently, no Gregorian in fullscreen
- Wants Surah names as dial identifiers, not color names
- Sensitive to color discontinuities between clock and background
- Wants NOMOS reference fidelity (sandblast, not sunray)

### Lessons Learned
- Always test on device — headless renders miss mobile-specific issues (crystal edge, color mismatch)
- Sequential API calls = bad UX. Race parallel paths
- Three.js scene.background + MeshBasicMaterial with same color still render differently due to tonemapping pipeline subtleties
- iOS Safari cache: `max-age=600` means 10 min stale possible, always bump version param
