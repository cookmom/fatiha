# 2026-03-08 Daily Log

## Session: Late Night AGOT UX + Autoresearch

### Chris Autoresearch Clean Run (00:05–00:35)
- Spawned Chris subagent to re-run autoresearch pipeline with CLEAN screenshots (no onboarding overlay)
- `eval.py` now dismisses `.clock-onboard` elements before capture
- Results: clean baseline aesthetic 61.9 → Chris best 71.0 (combined 79.7), FPS steady 60.2
- Best params hash: `57b1f592af` on `autoresearch/shader-lookdev` branch
- **Tawfeeq feedback**: Chris's changes made clock hands lose their laser-beam character and affected prayer window look. Wants beams + prayer windows LOCKED — only optimize cube material, lighting, fog next time.
- Chris timed out at 31 min but got enough iterations

### Branch Contamination Issue (recurring)
- Autoresearch stash operations AGAIN left shell on wrong branch
- Chris's `edgeCatch * 3` (invalid GLSL — should be `* 3.0`) bled into main via stash pop
- Fixed: reverted to `* 1.80` on main
- **LESSON**: Must check `git diff HEAD` after ANY stash pop to catch cross-branch contamination

### UI Batch (v367→v369, commits c490968→d5ac14c on main)
1. **Three-layer prayer labels**: name (colored) + time + info line (Obligatory / Sunnah Mu'akkadah / Avoid prayer)
   - Applied to BOTH resting `_currentPrayerLabel` AND `_swipeLabel`
   - Initially only swipe label got the update — current prayer label was missed on first push
2. **Header font size matching**: hijri date was smaller/dimmer than city+countdown
   - Changed from `clamp(.7rem,1.4vmin,.9rem)` opacity .5 → `clamp(.8rem,2vw,1rem)` opacity .65
   - Now all three header elements: 12.8px, weight 300, same color
3. **Prayer labels hide on mode switch**: `_currentPrayerLabel` + `_prayerDots` hidden immediately when switching to compass/info, restored on clock switch
   - Both JS (in `switchMode()`) and CSS (`body.mode-compass/info`) rules added

### Git State
- Main HEAD: `d5ac14c` (v369, cache rc-v239)
- Autoresearch branch: `autoresearch/shader-lookdev` has Chris's shader params (NOT merged)
- Next autoresearch run should LOCK beam params + prayer window params
